# Public build does not require any environment variables.
# The site runs with built-in mock data and localStorage persistence.

# If you later connect this frontend to your own backend,
# define your own API base URL in a private local `.env` file:
# VITE_API_BASE_URL=https://your-api.example.com
